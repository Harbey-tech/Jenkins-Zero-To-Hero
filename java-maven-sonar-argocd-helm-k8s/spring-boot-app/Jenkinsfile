pipeline {
    agent any
    stages {
        stage('Docker Test') {
            steps {
                script {  // <-- wrap non-declarative steps in script
                    docker.image('alpine:latest').inside {
                        sh 'echo "Hello from container"'
                    }
                }
            }
        }
    }
}
